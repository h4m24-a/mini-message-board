<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Update Form</title>
</head>
<body>
  <h1 class="title" >Update Form</h1>
  <div class="form-container">
    <form class="form" action="/update/<%= message.id %>" method="POST">
      <label class="input-group"  for="username">Username</label>
      <input id="username" type="text" name="messageUser"  value="<%= message.username %>" maxlength="20" required />
  
      <label class="input-group"  for="message">Message</label>
      <input id="message" type="text" name="messageText" value="<%= message.text %>" maxlength="150" required />
  
      <button class="angled-gradient-button3" type="submit">Update Message</button>
      <a class="angled-gradient-button3" href="/">Back to home</a>
    </form>
  </div>
</body>
















<style>
  @import url('https://fonts.googleapis.com/css2?family=Inter:wght@100;200;300;400;500;600;700;800;900&display=swap');
@import url('https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&display=swap');
@import url('https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&display=swap');


a {
  text-decoration: none;
}


.form {
  display: flex;
  flex-direction: column;
  background-color: white;
  padding: 40px;
  margin-top: 5px;
  border: black;
  border-radius: 8px;
  box-shadow: 0px 19px 38px 0px rgba(0,0,0,0.3), 0px 15px 12px 0px rgba(0,0,0,0.22);
}

/* Container for form inputs */
.input-group {
  background-color: #ffffff; /* Clean white background */
  border-radius: 12px; /* Larger border radius for a more rounded look */
  padding: 24px; /* Generous padding for a spacious feel */
  margin: 5px 0 30px 0;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); /* More pronounced shadow for depth */
}

/* Style for the form and text inputs */
form, input[type="text"] {
  font-family: 'Poppins', Arial, sans-serif; /* Modern font stack */
  font-size: 16px; /* Slightly smaller font size for a sleek look */
  color: #333; /* Dark gray color for readability */
  outline: none; /* Custom focus styling */
  margin-bottom: 16px; /* Reduced margin for a compact layout */
  border: 1px solid #ddd; /* Light border color for a minimalist design */
  border-radius: 8px; /* Rounded corners for inputs */
  background-color: #f9f9f9; /* Light gray background for inputs */
  padding: 12px; /* Comfortable padding inside inputs */
  transition: border-color 0.3s, box-shadow 0.3s; /* Smooth transitions for focus effects */
}

/* Style for form labels */
form label {
  color: black; /* Medium gray color for a modern look */
  font-size: 18px; /* Smaller font size for labels */
  margin-bottom: 8px; /* Space between label and input */
  font-weight: 500; /* Slightly lighter weight for a clean design */
  display: block; /* Ensures label is on its own line */
}

/* Add focus styles for inputs */
input[type="text"]:focus {
  border-color: #007bff; /* Bright blue border color on focus */
  box-shadow: 0 0 8px rgba(0, 123, 255, 0.25); /* Subtle glow effect */
}

/* Add placeholder styling */
input[type="text"]::placeholder {
  color: #aaa; /* Light gray placeholder text for a modern touch */
  opacity: 1; /* Ensure placeholder is visible */
}


.form-container {
  width: 30%;
  margin: 0 auto;
}



button {
  background-size: 220%;
  box-shadow: 0 .2em .3em rgba(0, 0, 0, 0.15);
  color: #3363ff;
  background-color: transparent;
  background-position: 100%;
  border: 3px solid #3363ff;
  text-transform: uppercase;
  padding: 8px 16px;
  border-radius: 3px;
  transition: all .2s ease-out;
  font-weight: 900;
  cursor: pointer;
  font-family: 'Roboto', Cambria, Cochin, Georgia, Times, 'Times New Roman', serif;
  letter-spacing: 2px;
  background-image: linear-gradient(110deg, #3363ff 0%, #3363ff 50%, transparent 50%, transparent 100%);
  font-size: 14px;
  display: block; /* Make sure it's a block-level element */
  margin: 10px auto; /* Center horizontally */
  max-width: 50%;
  text-align: center;
}

button:hover {
  transform: translateY(-4px);
  box-shadow: 0 0 .3em rgba(0, 0, 0, 0.25);
  background-position: 0;
  color: #fff;
}

button:active {
  transform: translateY(-1px);
}

.title {
  text-align: center;
  font-size: 40px;
  margin-top: 10px;
  color: black;
  font-family: 'Inter', Cambria, Cochin, Georgia, Times, 'Times New Roman', serif;
}

a {
  background-size: 220%;
  box-shadow: 0 .2em .3em rgba(0, 0, 0, 0.15);
  color: #34D821;
  font-family: 'Roboto', Cambria, Cochin, Georgia, Times, 'Times New Roman', serif;
  background-color: transparent;
  background-position: 100%;
  border: 3px solid #34D821;
  text-transform: uppercase;
  padding: 8px 16px;
  border-radius: 3px;
  transition: all .2s ease-out;
  font-weight: 900;
  cursor: pointer;
  letter-spacing: 2px;
  background-image: linear-gradient(110deg, #34D821 0%, #34D821 50%, transparent 50%, transparent 100%);
  font-size: 14px;
  max-width: 100%;
  width: 30%;
  text-align: center;
  margin: 10px auto;
}

a:hover {
  transform: translateY(-2px);
  box-shadow: 0 0 .3em rgba(0, 0, 0, 0.25);
  background-position: 0;
  color: #fff;
}

a:active {
  transform: translateY(-1px);
}


@media (max-width: 1200px) {
  .form-container {
    width: 50%; /* Adjust width for screens 1200px or smaller */
  }
}

@media (max-width: 768px) {
  .form-container {
    width: 70%; /* Adjust for tablet-sized screens */
  }
}

@media (max-width: 480px) {
  .form-container {
    width: 100%; /* Adjust for mobile devices */
    padding: 20px; /* Reduce padding for smaller screens */
  }

  form label {
    font-size: 16px; /* Slightly smaller labels on mobile */
  }

  input[type="text"], input[type="submit"] {
    font-size: 14px; /* Smaller font for inputs on mobile */
    padding: 10px; /* Reduce padding for better fit */
  }

  button, a {
  font-size: 14px;
  text-align: center;
}
}



</style>
</html>
